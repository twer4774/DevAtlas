services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: portal
      POSTGRES_PASSWORD: portal
      POSTGRES_DB: portal
    ports:
      - "54323:5432"
    volumes:
      - portal_pgdata:/var/lib/postgresql/data

volumes:
  portal_pgdata:
